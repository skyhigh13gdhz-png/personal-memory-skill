# Personal Memory

用于访问用户自己的长期外置记忆系统。

## 目标

通过 Memory Gateway 访问长期记忆。优先保持交互简单、结果可验证，不要声称已经写入或查询记忆，除非对应 Gateway 操作实际成功。

## 用户意图

### 记住这个

当用户明确要求记录、记住、保存或归档当前信息时，调用外置记忆的 retain 能力。只发送与本次记忆有关的必要上下文，避免无意义地重复保存整段历史聊天。

成功后简洁确认实际写入成功；失败时明确说明失败，不得假装已经记录。

### 查一下我的记忆

当用户主动要求查询过去的记录、经历、决定、项目进度、偏好或历史讨论时，使用 recall。查询语句应保留用户当前问题中的关键实体、时间和上下文。

将返回的记忆作为回答上下文，而不是机械原样堆给用户。

### 结合以前的记忆想一下

当用户明确需要跨多条历史记忆进行综合、归纳或反思时，使用 reflect。普通事实查找优先 recall，不要无意义地使用更慢的 reflect。

## 效率原则

- 普通聊天不访问外置记忆。
- 一次明确的记忆操作尽量只触发一次必要的 Gateway 路径。
- Recall 是常用快速路径；Reflect 是较重的深度路径。
- Retain 保存增量和关键内容，不默认重复保存完整 transcript。
- 不要求用户选择 bank、接口名称或技术参数。

## 安全

Skill 文件和公开仓库不得包含 Gateway API Token、服务器凭据、个人真实记忆、OAuth Secret 或其他秘密。
